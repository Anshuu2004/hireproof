import { VerifyFlow } from "@/components/verify/verify-flow";

export const metadata = {
  title: "Prove you're real — HireProof",
};

export default function VerifyPage() {
  return <VerifyFlow />;
}
